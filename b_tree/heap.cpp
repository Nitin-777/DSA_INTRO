/*heap is basically complete binary tree that comes with a heap order property
 CBT- *When a Btree has all max childrens in all levels except the last level
      * The nodes should always be entered from the left side 
Max Heap- when the child nodes will always have less value than the parent child. Min heap just the opposite*/
 
